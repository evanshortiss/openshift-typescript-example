<section class='p-8'>
  <h1 class="text-3xl">
    Node.js & TypeScript on OpenShift! 👩‍🎤
  </h1>
  <p class="pt-4 text-xl">
    {{#if viewCount}}
    View count: {{viewCount}}
    {{else}}
    Connect Redis to enable the page view counter.
    {{/if}}
  </p>
</section>

